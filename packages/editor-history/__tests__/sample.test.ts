import { describe, expect, it } from 'vitest'

import * as mod from '../src/index.js'

describe('@storywork/editor-history', () => {
  it('모듈이 import 됩니다', () => {
    expect(mod).toBeDefined()
  })
})
